/**
 * 选择排序
 */
const selectionSort = (arr) => {
  for(let i=0;i<arr.length;i++){
    let min = arr[0]
  }

  return arr;
};

console.log(selectionSort([1, 2, 3, 4, 5]));
console.log(selectionSort([3, 1, 4, 5, 2]));
console.log(selectionSort([2, 1, 1, 3, 2]));
